<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tahmid.hydroponicapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistrationActivity"
            android:label="Registration"/>

        <activity android:name=".DashBoard"
            android:label="DashBoard"/>

        <activity android:name=".SensorValues"
            android:label="Reading"/>
        <activity android:name=".AnimatedReadings"
            android:label="Readings"/>
        <activity android:name=".StatActivity"
            android:label="Statistics"/>

        <activity android:name=".pHStatActivity"
            android:label="pH Stats"/>

    </application>

</manifest>